<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to admin_area_title, admin_root_path, :class => 'brand' %>
      <% if admin_signed_in? %>
        <div class="container nav-collapse">
          <ul class="nav">
            <%= navigation_item(:users, admin_users_path) %>
            <%= navigation_item(:categories, admin_categories_path) %>
            <%= navigation_item(:reviews, admin_reviews_path) %>
          </ul>

          <ul class="nav pull-right">
            <li><%= link_to t('helpers.links.go_to_website'), root_path, :target => '_blank' %></li>

            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <i class="icon-user icon-white"></i> <%= current_admin %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <%= link_to "Profile", edit_admin_profile_path %>
                <%= link_to t('helpers.links.sign_out'), destroy_admin_session_path, :method => 'delete' %>
              </ul>
            </li>
          </ul>
        </div>
      <% end %>
    </div>
  </div>
</div>